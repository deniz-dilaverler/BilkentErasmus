package com.caddy.erasxchange.repositories.application;

import com.caddy.erasxchange.models.application.ErasmusApplicationCancel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ErasmusApplicationCancelRepository extends JpaRepository<ErasmusApplicationCancel, Long> {
}
