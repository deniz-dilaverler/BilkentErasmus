package com.caddy.erasxchange.models.accounts;

import javax.persistence.Entity;
import javax.persistence.Table;

@Entity
@Table(name = "iso_coordinator_account")
public class ISOCoordinatorAccount extends Account {
}