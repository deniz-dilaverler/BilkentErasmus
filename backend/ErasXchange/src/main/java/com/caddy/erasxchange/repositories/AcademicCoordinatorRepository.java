package com.caddy.erasxchange.repositories;

import com.caddy.erasxchange.models.AcademicCoordinator;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AcademicCoordinatorRepository extends JpaRepository<AcademicCoordinator, Long> {
}